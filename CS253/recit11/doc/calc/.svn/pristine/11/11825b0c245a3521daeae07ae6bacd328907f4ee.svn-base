#include <string>

const int NUM_REG=50;
class Register {
    public:
	Register() : name(""), value(0.0) {}
	std::string name;
	number value;
};

void clear_stack();
number push(number num);
number pop();
const int STACK_MAX=50;
extern number stack[STACK_MAX];
extern int stack_size;
extern int stack_max;
