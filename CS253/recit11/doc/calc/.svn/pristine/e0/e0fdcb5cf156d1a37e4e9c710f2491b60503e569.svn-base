#include "calc.h"

int exchange()
{
	number x,y;

	x = pop();
	y = pop();

	push(x);
	push(y);
	return SUCCESS;
}

static func_info fi(exchange, "exchange", "exchange x and y");
static func_info fi2(exchange, "xy", "exchange x and y");
